version: '3.9'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ems-frontend
    environment:
      - BACKEND_HOST=ems-backend:8443
    ports:
      - "8444:443"
      - "8080:80"
    volumes:
      - ./nginx/ssl:/etc/nginx/ssl:ro
    networks:
      - ems-network

networks:
  ems-network:
    external: true
